me_feels:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'mefeels') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'mefeels_dev') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'mefeels') }}"
      schema: analytics
      threads: 4
