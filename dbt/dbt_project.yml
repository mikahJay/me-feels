name: me_feels
version: '1.0.0'
config-version: 2

profile: me_feels

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  me_feels:
    analytics:
      +materialized: view
      +schema: analytics
